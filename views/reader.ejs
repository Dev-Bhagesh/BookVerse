<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <title><%= book.title %> — Reader</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            height: 100vh;
        }
        .pdf-container {
            flex: 1;
        }
        iframe {
            width: 100%;
            height: 100%;
            border: none;
        }
    </style>
</head>
<body>

    <h2 style="text-align:center; padding:10px;"><%= book.title %></h2>

    <div class="pdf-container">
        <iframe src="/<%= book.pdfpath %>"></iframe>
    </div>

</body>
</html>
